---
# role: ansible-role-timesyncd
# file: meta/main.yml

galaxy_info:
  role_name: ansible-role-timesyncd
  author: Jonas Mauer
  description: Ansible role for setting up systemd-timesyncd.
  company: ''

  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  issue_tracker_url: https://github.com/jam82/ansible-role-timesyncd/issues

  license: MIT

  min_ansible_version: 2.8

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  github_branch: master

  # platforms is a list of platforms, and each platform has a name and a list of versions.
  #
  platforms:
    - name: Archlinux
    - name: CentOS
      versions:
        - 8
    - name: Debian
      versions:
        - 9
        - 10
    - name: Fedora
      versions:
        - 31
    - name: OpenSuse Leap
      versions:
        - 15
    - name: Ubuntu
      versions:
        - 18.04
        - 20.04

  galaxy_tags: []

  dependencies: []
